apiVersion: v1 
kind: Service
metadata:
  name: mysql
  namespace: expense
spec:
  selector: # labels are used as selectors
    app: mysql
    tier: mysql
    project: expense
  ports:
  - protocol: TCP
    port: 3306 #service port
    targetPort: 3306